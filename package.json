{
  "name": "fullstack-challenge",
  "version": "1.0.0",
  "description": "Sistema de Gestion de Ordenes - FullStack Challenge",
  "private": true,
  "scripts": {
    "dev": "docker-compose up",
    "dev:build": "docker-compose up --build",
    "prod": "docker-compose -f docker-compose.prod.yml up",
    "prod:build": "docker-compose -f docker-compose.prod.yml up --build",
    "down": "docker-compose down",
    "clean": "docker-compose down -v",
    "test:backend": "cd backend && npm test",
    "test:frontend": "cd frontend && npm test",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "author": "Juan Ignacio Andrada Cabo"
}
